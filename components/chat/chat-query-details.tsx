'use client'

import { Text } from '@radix-ui/themes'

const ChatQueryDetails = () => {
  return (
    <Text>
      ChatQueryDetails ChatQueryDetails ChatQueryDetails ChatQueryDetails
      ChatQueryDetails ChatQueryDetails ChatQueryDetails ChatQueryDetails
    </Text>
  )
}

export default ChatQueryDetails
